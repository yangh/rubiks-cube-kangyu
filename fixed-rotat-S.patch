diff --git a/CMakeLists.txt b/CMakeLists.txt
index 04bc045..2891fdd 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -133,3 +133,17 @@ target_include_directories(test_axis_rotate
 
 # Add axis rotation test
 add_test(NAME axis_rotate COMMAND test_axis_rotate)
+
+# Create color validation test executable
+add_executable(test_color_validation
+    tests/test_color_validation.cpp
+    src/cube.cpp
+)
+
+target_include_directories(test_color_validation
+    PRIVATE
+    ${CMAKE_CURRENT_SOURCE_DIR}/src
+)
+
+# Add color validation test
+add_test(NAME color_validation COMMAND test_color_validation)
diff --git a/src/cube.cpp b/src/cube.cpp
index f2a7db1..3161e47 100644
--- a/src/cube.cpp
+++ b/src/cube.cpp
@@ -115,6 +115,12 @@ std::string colorToString(Color color) {
     }
 }
 
+bool isOppositeColor(Color a, Color b) {
+    return (a == Color::WHITE  && b == Color::YELLOW)  || (a == Color::YELLOW  && b == Color::WHITE) ||
+           (a == Color::RED    && b == Color::ORANGE)  || (a == Color::ORANGE  && b == Color::RED)   ||
+           (a == Color::GREEN  && b == Color::BLUE)    || (a == Color::BLUE    && b == Color::GREEN);
+}
+
 bool parseMoveString(const std::string& moveStr, Move& outMove) {
     // Trim whitespace
     std::string trimmed = moveStr;
@@ -580,20 +586,22 @@ void RubiksCube::rotateBack(bool prime) {
 // S (Standing): Rotate the middle slice between F and B
 void RubiksCube::rotateStanding(bool prime) {
     // Standing slice affects: Up[3,4,5], Down[3,4,5], Left[1,4,7], Right[1,4,7]
-    // Looking from front: S rotates up->right
+    // Edge rotation for S clockwise: DL->UL->UR->DR->DL
+    // Each edge piece's stickers rotate 90deg around F-B axis:
+    //   down->left, left->up, up->right, right->down
     if (prime) {
-        // S' (counter-clockwise from front): up -> left -> down -> right -> up
+        // S' (counter-clockwise from front): UL->DL->DR->UR->UL
         std::array<Color, 9> temp = up_;
-        up_[3] = right_[1]; up_[4] = right_[4]; up_[5] = right_[7];
-        right_[1] = down_[5]; right_[4] = down_[4]; right_[7] = down_[3];
+        up_[5] = right_[7]; up_[4] = right_[4]; up_[3] = right_[1];
+        right_[7] = down_[3]; right_[4] = down_[4]; right_[1] = down_[5];
         down_[3] = left_[1]; down_[4] = left_[4]; down_[5] = left_[7];
-        left_[1] = temp[3]; left_[4] = temp[4]; left_[7] = temp[5];
+        left_[1] = temp[5]; left_[4] = temp[4]; left_[7] = temp[3];
     } else {
-        // S (clockwise from front): up -> right -> down -> left -> up
+        // S (clockwise from front): DL->UL->UR->DR->DL
         std::array<Color, 9> temp = up_;
-        up_[5] = left_[1]; up_[4] = left_[4]; up_[3] = left_[7];
+        up_[3] = left_[7]; up_[4] = left_[4]; up_[5] = left_[1];
         left_[1] = down_[3]; left_[4] = down_[4]; left_[7] = down_[5];
-        down_[3] = right_[1]; down_[4] = right_[4]; down_[5] = right_[7];
+        down_[5] = right_[1]; down_[4] = right_[4]; down_[3] = right_[7];
         right_[1] = temp[3]; right_[4] = temp[4]; right_[7] = temp[5];
     }
 }
@@ -658,3 +666,47 @@ void RubiksCube::rotateZ(bool prime) {
         rotateBack(true);     // B'
     }
 }
+
+bool RubiksCube::isValidColorConfiguration() const {
+    return getValidationError().empty();
+}
+
+std::string RubiksCube::getValidationError() const {
+    // Edges: 12 pieces, each with 2 colors
+    // Format: {face1, index1, face2, index2, name}
+    struct EdgeDef { const std::array<Color, 9>* f1; int i1; const std::array<Color, 9>* f2; int i2; const char* name; };
+    EdgeDef edges[12] = {
+        {&up_, 7, &front_, 1, "UF"}, {&up_, 3, &left_, 1, "UL"},
+        {&up_, 5, &right_, 1, "UR"}, {&up_, 1, &back_, 1, "UB"},
+        {&down_, 1, &front_, 7, "DF"}, {&down_, 3, &left_, 7, "DL"},
+        {&down_, 5, &right_, 7, "DR"}, {&down_, 7, &back_, 7, "DB"},
+        {&front_, 3, &left_, 5, "FL"}, {&front_, 5, &right_, 3, "FR"},
+        {&back_, 3, &right_, 5, "BL"}, {&back_, 5, &left_, 3, "BR"}
+    };
+    
+    for (const auto& e : edges) {
+        if (isOppositeColor((*e.f1)[e.i1], (*e.f2)[e.i2])) {
+            return "Edge " + std::string(e.name) + " has opposite colors: " +
+                   colorToString((*e.f1)[e.i1]) + "-" + colorToString((*e.f2)[e.i2]);
+        }
+    }
+    
+    // Corners: 8 pieces, each with 3 colors
+    struct CornerDef { const std::array<Color, 9>* f1; int i1; const std::array<Color, 9>* f2; int i2; const std::array<Color, 9>* f3; int i3; const char* name; };
+    CornerDef corners[8] = {
+        {&up_, 6, &front_, 0, &left_, 2, "UFL"}, {&up_, 8, &front_, 2, &right_, 0, "UFR"},
+        {&up_, 0, &back_, 2, &left_, 0, "UBL"}, {&up_, 2, &back_, 0, &right_, 2, "UBR"},
+        {&down_, 0, &front_, 6, &left_, 8, "DFL"}, {&down_, 2, &front_, 8, &right_, 6, "DFR"},
+        {&down_, 6, &back_, 8, &left_, 6, "DBL"}, {&down_, 8, &back_, 6, &right_, 8, "DBR"}
+    };
+    
+    for (const auto& c : corners) {
+        Color c1 = (*c.f1)[c.i1], c2 = (*c.f2)[c.i2], c3 = (*c.f3)[c.i3];
+        if (isOppositeColor(c1, c2) || isOppositeColor(c1, c3) || isOppositeColor(c2, c3)) {
+            return "Corner " + std::string(c.name) + " has opposite colors: " +
+                   colorToString(c1) + "-" + colorToString(c2) + "-" + colorToString(c3);
+        }
+    }
+    
+    return "";
+}
diff --git a/src/cube.h b/src/cube.h
index 8fd6212..ecde18f 100644
--- a/src/cube.h
+++ b/src/cube.h
@@ -86,6 +86,10 @@ std::string colorToString(Color color);
 // Helper function to get inverse of a move (defined in main.cpp)
 Move getInverseMove(Move move);
 
+// Check if two colors are opposite on a standard Rubik's cube
+// Opposite pairs: WHITE-YELLOW, RED-ORANGE, GREEN-BLUE
+bool isOppositeColor(Color a, Color b);
+
 // Represents the 3x3x3 Rubik's cube state
 class RubiksCube {
 public:
@@ -146,6 +150,14 @@ public:
     // Check if redo is available
     bool canRedo() const { return !redoHistory_.empty(); }
 
+    // Validate cube color configuration
+    // Returns true if no edge/corner contains opposite colors
+    // Opposite pairs: WHITE-YELLOW, RED-ORANGE, GREEN-BLUE
+    bool isValidColorConfiguration() const;
+
+    // Get detailed validation error message (empty if valid)
+    std::string getValidationError() const;
+
 private:
     std::array<Color, 9> front_;
     std::array<Color, 9> back_;
